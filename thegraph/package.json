{
  "name": "nearmessage",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ vlodkomr/nearmessage",
    "create-local": "graph create --node http://localhost:8020/ vlodkomr/nearmessage",
    "remove-local": "graph remove --node http://localhost:8020/ vlodkomr/nearmessage",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 vlodkomr/nearmessage",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.34.0",
    "@graphprotocol/graph-ts": "0.28.0"
  }
}
